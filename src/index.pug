doctype html

html(lang=en)
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(name="theme-color", content="#009688")
    title AM-Kit
  body
    .content
      .card#code
        .editor-title-bar
          .editor-title Code
          .editor-run
            svg.control-reset.animation-control--hide(xmlns="http://www.w3.org/2000/svg", viewBox="0 0 24 24", width="24px", height="24px")
              defs
                linearGradient(id="grad1", x1="0%", y1="0%", x2="100%", y2="0%")
                  stop(offset="0%", style="stop-color:rgb();stop-opacity:1")
                  stop(offset="100%", style="stop-color:rgb();stop-opacity:1")
              polygon(points="4,4 20,12 4,20", stroke-width="2.6", fill="#00CB94", stroke-linecap="round", stroke-linejoin="round")

        .code-editor#editor
        //#editor
      .sub-layout
        .card#graph
          | Graph
        .card#animation
          | Animation

